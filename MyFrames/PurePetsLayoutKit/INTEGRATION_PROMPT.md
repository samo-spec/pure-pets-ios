# AI Integration Prompt

Inspect the complete Pure Pets iOS project before editing. Integrate the attached `PurePetsLayoutKit` package as the replacement architecture for `PPCollectionLayoutManager` and `PPPinterestLayout`, but preserve every existing business rule, Firebase operation, API, model, delegate callback, permission check, navigation route, analytics event, media behavior, and user-visible function.

Work only on the layout migration and files directly required to compile it. Do not rename models, enums, selectors, Firestore fields, collections, reuse identifiers, notifications, or public Objective-C APIs. Do not delete the old `.h/.m` files initially. First map every caller and dependency. Then remove only `PPCollectionLayoutManager.m` and `PPPinterestLayout.m` from the target's Compile Sources after confirming that the Swift bridge classes expose the same Objective-C runtime names and selectors. Keep the compatibility headers until all Objective-C callers are migrated.

Create one strongly typed adapter from the real `PPUniversalCellViewModel` API to `PPLayoutItemDescriptor`. Use each item's own stable identifier, source image size, preferred aspect ratio, title/subtitle/location state, context, and real card-body requirements. Never calculate all cells from the first item. Never use `UIScreen.main.bounds` for item geometry. Never clamp unrelated images to `0.78...0.92`. Never force Pinterest cards to be square. Never index a multi-section source with `items[indexPath.item]`; resolve both section and item. Do not use KVC in the final app adapter when real typed properties are available.

For existing UIKit screens, wire `PPCollectionLayoutManager` from the package without changing controller business logic. Set `items`, provide the typed descriptor/stable-ID closures, preserve visible scroll position during layout switches, and call `invalidateLayout(in:indexPaths:)` when asynchronous image metadata changes. Use the corrected Swift `PPPinterestLayout` for masonry behavior. Preserve diffable-data-source safety and stable IDs.

For screens approved for SwiftUI migration, embed `PPLayoutContainer` through `UIHostingController` or `UIHostingConfiguration` as appropriate. Keep existing manager/coordinator objects as the source of truth; do not duplicate Firebase listeners or move security/business decisions into views. Implement the existing universal card behavior in SwiftUI using `PPPremiumCardSurface`, semantic typography, Dynamic Type, safe-area-aware spacing, leading/trailing alignment, VoiceOver labels/actions, Arabic RTL, Reduce Motion, Reduce Transparency, loading/empty/error states, keyboard dismissal, press feedback, and native navigation. Default marketplace media to aspect-fit so the complete image is visible. Use aspect-fill only where the current approved design explicitly requires intentional crop.

Do not create a second design system. Reuse the project's real colors, fonts, spacing tokens, localization keys, image loader/cache, favorite/cart managers, delegates, and analytics. Do not hardcode user-facing English or Arabic strings. Do not introduce third-party layout or image dependencies.

Build and run on the already-connected iPhone 13 Pro Max only. Do not use a simulator or another device. Test every layout mode with portrait, square, landscape, missing-size, delayed-size, video, long-title, empty, loading, error, Arabic RTL, English LTR, dark mode, light mode, accessibility Dynamic Type, VoiceOver, Reduce Motion, rotation, background/foreground, rapid layout switching, pagination, reloads, and multi-section data. Take screenshots before and after each migrated screen and compare geometry and intended appearance. Use Instruments/Core Animation or Xcode performance tools to verify scrolling hitches, layout passes, memory, image decoding, and offscreen rendering; do not claim 120 Hz without measurement.

Acceptance requirements: zero build errors, zero new warnings, zero crashes, zero constraint warnings, no incorrect top/bottom image crop in aspect-fit mode, no first-item sizing leakage, no wrong-section item lookup, no lost scroll position, no duplicated listeners, no permission or business-logic regression, and visually cohesive native behavior across all supported devices and accessibility settings. Keep the legacy implementation available for rollback until all acceptance gates pass, then provide a concise migration report listing edited files, preserved APIs, tests performed, measurements, remaining risks, and rollback steps.
