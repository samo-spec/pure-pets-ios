name: Runner Smoke
on:
  push:
    branches: [feat/order-living-handoff-rail]
    paths: [.github/workflows/runner-smoke.yml]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo ready
