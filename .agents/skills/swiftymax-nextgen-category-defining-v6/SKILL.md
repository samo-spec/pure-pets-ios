---
name: swiftymax-nextgen-category-defining-v6
description: Project-local launcher for the canonical SwiftyMax NextGen Category-Defining V6 package in PPAgent. Use for Pure Pets iOS/SwiftUI/UIKit creation, redesign, refactor, polish, motion, accessibility, RTL, design-system work, rendered critique, or world-class UI evaluation.
---

# SwiftyMax NextGen Category-Defining V6 — Project Launcher

Before any iOS UI/UX work, resolve and read the canonical V6 `SKILL.md` in this order:

1. `$PPAGENT_ROOT/swiftymax-nextgen-category-defining-v6/SKILL.md` when `PPAGENT_ROOT` is set.
2. `../PPAgents/swiftymax-nextgen-category-defining-v6/SKILL.md`.
3. `../PPAgent/swiftymax-nextgen-category-defining-v6/SKILL.md`.
4. `.agents/vendor/swiftymax-nextgen-category-defining-v6/SKILL.md`, created by `scripts/install_ppagent_skills.sh`.

If the source directory is unavailable but the sibling PPAgent checkout contains `swiftymax-nextgen-category-defining-v6.zip`, run `scripts/install_ppagent_skills.sh` to extract a clean project-local copy and then load the canonical skill and its required references.

The canonical V6 package is authoritative. This launcher exists only to make the skill discoverable from `pure-pets-ios`; do not invent V6 rules when the canonical source cannot be resolved.

For work that also changes Firebase, PPOrder, Chats, Notifications, authorization, or backend state, load `$pure-pets-firebase-mission-control-v3` as well. Mission Control V3 owns backend semantics; V6 owns the iOS experience and presentation.
